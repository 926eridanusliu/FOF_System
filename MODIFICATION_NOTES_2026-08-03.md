# 2026-08-03 修改说明

## 本次完成

1. 附件 1-1 和附件 1-2 的填写页面统一显示必填星号。只有题目文字中明确含“（如有）”的字段不标星，页面左下角持续显示小字说明。
2. 选择产品投资策略后，只显示对应的量化、CTA、债券或期权专属问题；未选中分支不出现可填写问题。
3. 所有表格均改用真实业务标题、行名和列名，并按日期、人数、比例、邮箱、电话等类型提供相应输入控件和格式提示。
4. 部门、股东、年度人数、核心成员和代表产品等可变表格可无限添加或删除行；超过 Word 模板原预留容量时，生成引擎自动复制模板格式并追加新行。
5. 策略代表产品表格也受策略筛选控制，未选择对应策略时不显示。
6. 管理方提交前执行全量必填和格式校验；未完成时返回具体问题且不会锁定填写链接。
7. 公司方可对每一条管理方填写链接选择“允许修改”或“禁止修改”。管理方成功提交后自动禁止，公司方可再次开放。
8. 旧版 SQLite/PostgreSQL 数据库启动时会自动增加 `can_edit` 字段，无需删除或重建原数据库。
9. Windows/Nginx 内网部署说明已补充管理端和管理方填写端的权限边界，并明确要求设置 `PUBLIC_FRONTEND_URL`。
10. 修复旧版问卷 JSON 应用后表格仍显示导入前空白缓存的问题；导入完成后表格会按报告更新时间重新加载。
11. JSON 中可变表格不再受 Word 预留行数截断；远澜数据中的 5 个 CTA 代表产品均可导入。
12. 百分比和常用人数/年限单位在在线表格中转换为纯数字，Word 输出时自动恢复 `%`。
13. 数字表格字段改为普通文本式输入框，不再显示加减按钮，并继续进行整数、数值和百分比校验。
14. “新增产品”弹窗增加“单个新增 / 批量新增”模式：单个模式使用“产品类型 / 产品策略”，批量模式使用“共同产品类型 / 共同产品策略”。
15. 批量新增支持逐行填写产品名称和成立日期、增删行以及从 Excel 粘贴；共同类型和策略只需选择一次。
16. 批量创建由后端在同一事务内完成；发现重名或数据错误时整批不写入，避免只创建一部分产品。
17. 修复传统问卷 JSON 导入后，封面多产品名称被数据库单个关联产品名称覆盖的问题；管理人名称仍以系统关联管理人为准。
18. 补齐量化、债券和期权策略专属问题映射，并兼容 CTA 的 `position_sizing_method`、`data_sources_used` 字段名。
19. 动态表格在 Word 中会按实际填写行数删去未使用的模板预留空行；超出容量时仍按模板格式追加。
20. 代表产品和产品管理概况的规模字段在 Word 输出中自动补“亿”，输入端继续只接收数字。
21. 封面和表格截止日期统一输出为 `年.月.日`，公司成立日期输出为中文年月日；金融机构白名单恢复“是，机构名称”格式。
22. Word 生成后若出现遗漏、值不一致、多余填充、格式或表格结构问题，后端不再将该文件报告为生成成功。
23. 本次未修改图片上传、图片书签和图片填充功能。
24. “新增产品”的产品策略按 Word 封面顺序分为股票多空、相对价值、债券、管理期货、复合和其他六行，所有名称与报告正式名称一致。
25. 单个新增、批量新增和编辑产品统一使用 920px 宽度及相同的可视内容高度，切换模式时弹窗外框不再变化。
26. 产品选择“其他投资策略”后会写入报告的其他策略说明，并将 Word 封面对应的 `□` 自动改为 `☑`。

## 字段检查结果

- 附件 1-1（私募基金）：376 个清单字段，其中 2 项精确含“（如有）”，均为“补充说明（如有）”。
- 附件 1-2（持牌金融机构）：69 个清单字段，没有精确含“（如有）”的项目。

## 验证

- 后端自动化测试：33 项通过（含多产品 JSON 导入、四策略映射、其他策略封面勾选、动态表格缩减与扩展、单位恢复及 CTA 百分比测试）。
- 前端自动化测试：12 项通过（含产品策略分组、顺序及正式名称测试）。
- Vue/TypeScript 生产构建：通过。
- 使用 `yuanlan_data_corrected.json` 实际生成 19 页 Word；178 个非空字段全部一致，遗漏/不一致/多余/格式/表格问题均为 0。
